cp .env.example .env 

.env:

DB_HOST=mysql
DB_USER=root
DB_PASSWORD=root
DB_DATABASE=test

DB_MIGRATION_HOST=localhost

JWT_SECRET=sample_secret


docker-compose up

npm i

npm run migrations-up

Postman

<3
